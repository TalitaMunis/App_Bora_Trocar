name: bora_trocar
description: "A new Flutter project."
publish_to: "none" # Remove esta linha se quiser publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.1.1
  intl: ^0.18.1

  provider: ^6.1.2

  # HIVE (versões estáveis 100% compatíveis entre si)
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

  # Build runner e generator compatíveis com Hive 2.x
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

  # Exemplo de assets (descomente se usar)
  # assets:
  #   - images/

  # Exemplo de fontes (descomente se usar)
  # fonts:
  #   - family: Schyler
  #   # ...
