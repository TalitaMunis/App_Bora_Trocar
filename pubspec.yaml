name: bora_trocar
description: "A new Flutter project."
publish_to: "none" # Remove esta linha se quiser publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.1.1
  intl: ^0.18.1
  provider: ^6.1.2
  crypto: ^3.0.3
  uuid: ^4.5.0
  geolocator: ^11.0.0 # ✅ Adicionado para a funcionalidade de localização

  # HIVE (versões estáveis 100% compatíveis entre si)
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

flutter_icons:
  android: true
  ios: true
  web:
    generate: true
    image_path: "assets/icon/icon.png"
    background_color: "#hexcolor"
    theme_color: "#hexcolor"
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21

# Exemplo de assets (descomente se usar)
flutter:
  uses-material-design: true
  assets:
    - assets/icon/
# Exemplo de fontes (descomente se usar)
# fonts:
#   - family: Schyler
#     fonts:
#       - asset: fonts/Schyler-Regular.ttf
